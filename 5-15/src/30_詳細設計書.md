# 詳細設計書

## 1. クラス図

```mermaid
classDiagram
    VocabularyApp --> WordManager
    VocabularyApp --> Quiz
    VocabularyApp --> FileHandler
    VocabularyApp --> DBManager
    WordManager --> Word
    WordManager --> DBManager
    Quiz --> WordManager
    
    class VocabularyApp {
        -WordManager wordManager
        -Quiz quiz
        -FileHandler fileHandler
        -Scanner scanner
        -DBManager dbManager
        +VocabularyApp()
        +start()
        -registerWord()
        -startQuiz()
        -importWords()
        -exportWords()
        -deleteWord()
        -updateWord()
        -cleanup()
        +main(String[] args)
    }
    
    class Word {
        -String english
        -String japanese
        +Word(String english, String japanese)
        +getEnglish()
        +getJapanese()
        +setEnglish(String english)
        +setJapanese(String japanese)
    }
    
    class WordManager {
        -DBManager dbManager
        +WordManager(DBManager dbManager)
        +addWord(Word word)
        +getWords()
        +getWordCount()
        +deleteWord(String english)
        +updateWord(String english, String newJapanese)
    }
    
    class Quiz {
        -WordManager wordManager
        -int score
        -int totalQuestions
        +Quiz(WordManager wordManager)
        +checkAnswer(Word word, String answer)
        +getRandomWord()
        +getScore()
        +getTotalQuestions()
    }
    
    class FileHandler {
        +exportToCSV(List~Word~ words, String filename)
        +importFromCSV(String filename, WordManager wordManager)
    }
    
    class DBManager {
        -Connection connection
        -String URL
        -String USER
        -String PASSWORD
        +DBManager()
        -initializeDatabase()
        +getConnection()
        +close()
    }
```

## 2. シーケンス図

### 2.1 単語登録機能

```mermaid
sequenceDiagram
    participant User
    participant VocabularyApp
    participant WordManager
    participant DBManager
    participant Word
    
    User->>VocabularyApp: 単語登録選択
    VocabularyApp->>User: 英単語入力要求
    User->>VocabularyApp: 英単語入力
    VocabularyApp->>User: 日本語訳入力要求
    User->>VocabularyApp: 日本語訳入力
    VocabularyApp->>Word: 新規Word作成
    VocabularyApp->>WordManager: addWord(word)
    WordManager->>DBManager: getConnection()
    DBManager-->>WordManager: Connection
    WordManager->>DBManager: SQL実行（INSERT）
    VocabularyApp->>User: 登録完了表示
```

### 2.2 クイズ機能

```mermaid
sequenceDiagram
    participant User
    participant VocabularyApp
    participant Quiz
    participant WordManager
    participant DBManager
    
    User->>VocabularyApp: クイズ開始
    VocabularyApp->>WordManager: getWordCount()
    WordManager->>DBManager: SQL実行（COUNT）
    DBManager-->>WordManager: 単語数
    alt 単語が0個の場合
        VocabularyApp->>User: エラーメッセージ表示
    else 単語が存在する場合
        loop 登録単語数分
            VocabularyApp->>Quiz: getRandomWord()
            Quiz->>WordManager: getWords()
            WordManager->>DBManager: SQL実行（SELECT）
            DBManager-->>WordManager: 単語リスト
            WordManager-->>Quiz: 単語リスト
            Quiz-->>VocabularyApp: ランダムな単語
            VocabularyApp->>User: 問題表示
            User->>VocabularyApp: 解答入力
            VocabularyApp->>Quiz: checkAnswer(word, answer)
            Quiz-->>VocabularyApp: 判定結果
            VocabularyApp->>User: 結果表示
        end
        VocabularyApp->>Quiz: getScore(), getTotalQuestions()
        VocabularyApp->>User: 最終成績表示
    end
```

### 2.3 ファイル操作機能

```mermaid
sequenceDiagram
    participant User
    participant VocabularyApp
    participant FileHandler
    participant WordManager
    participant DBManager
    
    alt インポート処理
        User->>VocabularyApp: インポート選択
        VocabularyApp->>User: インポート選択
        VocabularyApp->>User: ファイル名入力要求
        User->>VocabularyApp: ファイル名入力
        VocabularyApp->>FileHandler: importFromCSV(filename, wordManager)
        FileHandler->>WordManager: addWord()
        WordManager->>DBManager: SQL実行（INSERT）
        FileHandler-->>VocabularyApp: 処理結果
        VocabularyApp->>User: 結果表示
    else エクスポート処理
        User->>VocabularyApp: エクスポート選択
        VocabularyApp->>User: エクスポート選択
        VocabularyApp->>User: ファイル名入力要求
        User->>VocabularyApp: ファイル名入力
        VocabularyApp->>WordManager: getWords()
        WordManager->>DBManager: SQL実行（SELECT）
        DBManager-->>WordManager: 単語リスト
        WordManager-->>VocabularyApp: 単語リスト
        VocabularyApp->>FileHandler: exportToCSV(words, filename)
        FileHandler-->>VocabularyApp: 処理結果
        VocabularyApp->>User: 結果表示
    end
```

### 2.4 単語削除機能

```mermaid
sequenceDiagram
    participant User
    participant VocabularyApp
    participant WordManager
    participant DBManager
    
    User->>VocabularyApp: 単語削除選択
    VocabularyApp->>User: 英単語入力要求
    User->>VocabularyApp: 英単語入力
    VocabularyApp->>WordManager: deleteWord(english)
    WordManager->>DBManager: SQL実行（DELETE）
    VocabularyApp->>User: 削除完了表示
```

### 2.5 単語更新機能

```mermaid
sequenceDiagram
    participant User
    participant VocabularyApp
    participant WordManager
    participant DBManager
    
    User->>VocabularyApp: 単語更新選択
    VocabularyApp->>User: 英単語入力要求
    User->>VocabularyApp: 英単語入力
    VocabularyApp->>User: 新日本語訳入力要求
    User->>VocabularyApp: 新日本語訳入力
    VocabularyApp->>WordManager: updateWord(english, newJapanese)
    WordManager->>DBManager: SQL実行（UPDATE）
    VocabularyApp->>User: 更新完了表示
```
